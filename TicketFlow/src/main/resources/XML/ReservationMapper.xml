<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.sunpark.ticketflow.DAO.ReservationDAO">
    <update id="reservationConfirm" parameterType="ReservationDTO">
        UPDATE reservation_seat SET
        status = "CONFIRMED"
        WHERE event_id = #{eventId}
        AND seat_number = #{seatNumber}
        AND user_id = #{userId}
    </update>
</mapper>